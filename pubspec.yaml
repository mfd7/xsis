name: xsis
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.6.6
  carousel_slider: ^5.0.0
  freezed_annotation: ^2.4.4
  dartz: ^0.10.1
  logger: ^2.4.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  alice_get_connect:
    path: 'packages/alice_get_connect'
  google_fonts: ^6.2.1
  overlay_support: ^2.1.0
  shimmer: ^3.0.0
  video_player: ^2.9.2
  youtube_player_iframe: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
